# 🎨 Visual System Architecture

## 🏗️ System Overview Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER'S BROWSER                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              REACT FRONTEND                          │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │  │
│  │  │  Pages   │  │Components│  │   API    │          │  │
│  │  │  (JSX)   │  │  (JSX)   │  │  (JS)    │          │  │
│  │  └──────────┘  └──────────┘  └──────────┘          │  │
│  │       │             │             │                 │  │
│  │       └─────────────┼─────────────┘                 │  │
│  │                     │                                │  │
│  │              ┌──────▼──────┐                        │  │
│  │              │   Contexts  │                        │  │
│  │              │  (State)    │                        │  │
│  │              └─────────────┘                        │  │
│  └──────────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ HTTP Requests (REST API)
                        │ WebSocket (Socket.io)
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              NODE.JS + EXPRESS BACKEND                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐        │  │
│  │  │  Routes  │→ │Middleware │→ │Controller│        │  │
│  │  │  (JS)    │  │   (JS)    │  │   (JS)   │        │  │
│  │  └──────────┘  └──────────┘  └────┬─────┘        │  │
│  │                                    │                │  │
│  │                              ┌─────▼─────┐         │  │
│  │                              │  Database │         │  │
│  │                              │   Config  │         │  │
│  │                              └─────┬─────┘         │  │
│  └────────────────────────────────────┼────────────────┘  │
└───────────────────────────────────────┼───────────────────┘
                                        │
                                        │ SQL Queries
                                        │
┌───────────────────────────────────────▼───────────────────┐
│                    MYSQL DATABASE                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  users   │  │customers  │  │ vehicles │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  orders  │  │ services │  │employees │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└───────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete Request Flow

### Example: Customer Creates Service Order

```
┌──────────────────────────────────────────────────────────────┐
│ STEP 1: USER INTERACTION                                     │
│ CustomerDashboard.jsx → Customer clicks "Request Service"     │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 2: NAVIGATION                                            │
│ React Router navigates to /customer/orders/new               │
│ Component: CustomerNewOrder.jsx renders                       │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 3: FORM SUBMISSION                                       │
│ User fills form and clicks "Submit"                          │
│ handleSubmit() function called                                │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 4: API CALL                                              │
│ ordersAPI.create(orderData)                                  │
│ Location: frontend/src/api/orders.js                          │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 5: HTTP REQUEST                                          │
│ POST http://localhost:5000/api/orders                        │
│ Headers: { Authorization: "Bearer <token>" }                 │
│ Body: { customer_id, vehicle_id, service_ids, ... }          │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 6: ROUTE HANDLER                                         │
│ File: backend/routes/orders.js                                │
│ router.post("/", authenticateCustomer, createOrder)           │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 7: AUTHENTICATION MIDDLEWARE                             │
│ File: backend/middleware/customerAuth.js                      │
│ - Extracts token from header                                  │
│ - Verifies JWT token                                          │
│ - Adds customer info to req.customer                          │
│ - If valid → Continue                                        │
│ - If invalid → Return 401                                    │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 8: CONTROLLER                                            │
│ File: backend/controllers/ordersController.js                 │
│ Function: createOrder()                                       │
│ - Validates data                                              │
│ - Gets database connection                                    │
│ - Starts transaction                                          │
│ - Inserts into orders table                                   │
│ - Links services in order_services table                      │
│ - Commits transaction                                         │
│ - Emits Socket.io event                                       │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 9: DATABASE OPERATION                                    │
│ File: backend/config/database.js                               │
│ pool.execute("INSERT INTO orders ...")                         │
│ MySQL executes SQL query                                       │
│ Returns new order ID                                           │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 10: SOCKET.IO BROADCAST                                  │
│ File: backend/controllers/ordersController.js                  │
│ io.emit("order:status", { orderId, status: "Received" })     │
│ All connected clients receive update                          │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 11: RESPONSE                                              │
│ Controller returns JSON:                                      │
│ { message: "Order created", order: {...} }                    │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 12: FRONTEND RECEIVES RESPONSE                            │
│ ordersAPI.create() resolves with response                      │
│ Toast notification shows "Order created successfully!"        │
│ Navigate to customer dashboard                                 │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 13: REAL-TIME UPDATE                                      │
│ SocketContext receives "order:status" event                    │
│ Shows toast: "Your order has been received"                    │
│ CustomerDashboard automatically refreshes order list          │
└──────────────────────────────────────────────────────────────┘
```

---

## 📂 File Organization Tree

```
A_to_Z_Auto_Repair/
│
├── backend/                          # Server-side code
│   ├── server.js                     # 🚀 Main entry point
│   ├── package.json                  # Dependencies
│   │
│   ├── config/                      # Configuration files
│   │   ├── database.js              # 🔌 Database connection
│   │   └── initDatabase.js          # 🗄️ Database setup
│   │
│   ├── controllers/                 # Business logic
│   │   ├── authController.js        # 🔐 Admin auth
│   │   ├── customerAuthController.js # 🔐 Customer auth
│   │   ├── customersController.js   # 👥 Customer CRUD
│   │   ├── ordersController.js      # 📋 Order management
│   │   ├── employeesController.js   # 👨‍💼 Employee management
│   │   ├── servicesController.js    # 🔧 Service management
│   │   ├── chatbotController.js    # 🤖 AI chatbot
│   │   └── recommendationsController.js # 💡 Service suggestions
│   │
│   ├── routes/                      # API endpoints
│   │   ├── auth.js                 # /api/auth/*
│   │   ├── customerAuth.js         # /api/customer-auth/*
│   │   ├── customers.js            # /api/customers/*
│   │   ├── orders.js               # /api/orders/*
│   │   ├── employees.js            # /api/employees/*
│   │   ├── services.js             # /api/services/*
│   │   ├── chatbot.js              # /api/chatbot/*
│   │   └── recommendations.js     # /api/recommendations/*
│   │
│   └── middleware/                   # Middleware functions
│       ├── auth.js                # 🔒 Admin auth check
│       └── customerAuth.js        # 🔒 Customer auth check
│
└── frontend/                        # Client-side code
    ├── package.json                # Dependencies
    ├── index.html                  # HTML template
    │
    └── src/
        ├── index.jsx               # 🚀 React entry point
        ├── index.css               # 🎨 Global styles
        ├── App.jsx                 # 📱 Main app component
        ├── App.css                 # App styles
        │
        ├── api/                    # API functions
        │   ├── config.js          # ⚙️ Axios setup
        │   ├── auth.js            # 🔐 Auth API
        │   ├── customers.js       # 👥 Customer API
        │   ├── orders.js          # 📋 Order API
        │   ├── services.js        # 🔧 Service API
        │   ├── employees.js       # 👨‍💼 Employee API
        │   ├── chatbot.js         # 🤖 Chatbot API
        │   ├── recommendations.js # 💡 Recommendations API
        │   └── index.js           # 📦 Central export
        │
        ├── components/             # Reusable components
        │   ├── Header.jsx         # 📄 Top navigation
        │   ├── Footer.jsx         # 📄 Bottom footer
        │   ├── LoginForm.jsx      # 🔐 Admin login
        │   ├── SignupForm.jsx     # 📝 Admin signup
        │   ├── CustomerLoginForm.jsx # 🔐 Customer login
        │   ├── CustomerSignupForm.jsx # 📝 Customer signup
        │   ├── AdminSidebar.jsx   # 📋 Admin menu
        │   ├── ThemeToggle.jsx    # 🌓 Theme switcher
        │   ├── AIChatbot.jsx      # 🤖 Chatbot UI
        │   ├── ForgotPassword.jsx # 🔑 Password recovery
        │   └── ForgotUsername.jsx # 👤 Username recovery
        │
        ├── pages/                  # Full page components
        │   ├── HomePage.jsx       # 🏠 Landing page
        │   ├── Dashboard.jsx     # 📊 Admin dashboard
        │   ├── CustomersList.jsx  # 📋 Customer list
        │   ├── CustomerDetail.jsx # 👤 Customer details
        │   ├── AddCustomer.jsx    # ➕ Add customer
        │   ├── EditCustomer.jsx   # ✏️ Edit customer
        │   ├── OrdersList.jsx     # 📋 Orders list
        │   ├── NewOrder.jsx       # ➕ Create order
        │   ├── OrderDetail.jsx    # 📄 Order details
        │   ├── EmployeesList.jsx  # 📋 Employees list
        │   ├── AddEmployee.jsx    # ➕ Add employee
        │   ├── EditEmployee.jsx   # ✏️ Edit employee
        │   ├── ServicesList.jsx   # 📋 Services list
        │   ├── CustomerDashboard.jsx # 🏠 Customer dashboard
        │   ├── CustomerAddVehicle.jsx # 🚗 Add vehicle
        │   └── CustomerNewOrder.jsx # 📋 Request service
        │
        └── contexts/              # Global state
            ├── ThemeContext.jsx   # 🌓 Theme management
            └── SocketContext.jsx  # 📡 Real-time connection
```

---

## 🔐 Authentication Flow Diagram

```
┌──────────────┐
│   User Login  │
└──────┬───────┘
       │
       ▼
┌─────────────────────┐
│  LoginForm.jsx      │
│  User enters email/ │
│  password           │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  adminAuthAPI.login │
│  (api/auth.js)      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  POST /api/auth/    │
│  login              │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  authController.js  │
│  login()            │
│  1. Check email     │
│  2. Verify password │
│  3. Create JWT      │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Returns:           │
│  { token, user }    │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  Frontend stores:   │
│  localStorage.setItem│
│  ('token', token)   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│  All future requests│
│  include token in   │
│  Authorization header│
└─────────────────────┘
```

---

## 📡 Real-Time Update Flow

```
┌─────────────────────────────────────────┐
│  Admin updates order status             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  ordersController.updateOrder()        │
│  - Updates database                     │
│  - Emits Socket.io event                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  Socket.io Server                       │
│  io.emit('order:status', data)          │
└──────────────┬──────────────────────────┘
               │
               ├─────────────────┐
               │                 │
               ▼                 ▼
┌──────────────────────┐  ┌──────────────────────┐
│  Customer Browser    │  │  Admin Browser       │
│  SocketContext       │  │  SocketContext       │
│  receives event      │  │  receives event      │
└──────────┬───────────┘  └──────────┬──────────┘
           │                          │
           ▼                          ▼
┌──────────────────────┐  ┌──────────────────────┐
│  Shows toast:        │  │  Shows toast:         │
│  "Order updated"     │  │  "Order updated"      │
│  Refreshes list      │  │  Refreshes list       │
└──────────────────────┘  └──────────────────────┘
```

---

## 🗄️ Database Relationships

```
┌─────────────┐
│   users      │ (Admin accounts)
│   id         │
│   email       │
│   password   │
└─────────────┘

┌─────────────┐
│  customers  │ (Customer accounts)
│   id         │
│   email      │
│   password   │
│   first_name │
│   last_name  │
└──────┬──────┘
       │
       │ 1:many
       │
       ├──────────────┐
       │              │
       ▼              ▼
┌─────────────┐  ┌─────────────┐
│  vehicles    │  │   orders    │
│   id         │  │   id        │
│   customer_id│  │   customer_id│
│   make       │  │   vehicle_id│
│   model      │  │   status    │
│   year       │  │   total_amount│
│   mileage    │  └──────┬──────┘
└─────────────┘          │
                         │ many:many
                         │
                         ▼
                  ┌──────────────┐
                  │order_services│
                  │  order_id   │
                  │  service_id │
                  └──────┬──────┘
                         │
                         │ many:1
                         │
                         ▼
                  ┌─────────────┐
                  │  services   │
                  │   id        │
                  │   name      │
                  │   description│
                  └─────────────┘
```

---

## 🎨 Component Hierarchy

```
App.jsx
│
├── Router
│   │
│   ├── HomePage
│   │   ├── Header
│   │   ├── Hero Section
│   │   ├── Services Section
│   │   ├── About Section
│   │   ├── Contact Section
│   │   └── Footer
│   │
│   ├── LoginPage
│   │   ├── Header
│   │   ├── LoginForm / SignupForm
│   │   └── Footer
│   │
│   ├── Dashboard (Admin)
│   │   ├── Header
│   │   ├── AdminSidebar
│   │   ├── Dashboard Content
│   │   └── Footer
│   │
│   ├── CustomersList
│   │   ├── Header
│   │   ├── AdminSidebar
│   │   ├── Customers Table
│   │   └── Footer
│   │
│   └── CustomerDashboard
│       ├── Header
│       ├── Welcome Section
│       ├── Vehicles Section
│       ├── Recommendations Section
│       ├── Orders Section
│       └── Footer
│
└── AIChatbot (Floating - Always visible)
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────────┐
│  Component State (useState)         │
│  - Local to component               │
│  - Example: form inputs, loading    │
└─────────────────────────────────────┘
              │
              │ When needed globally
              ▼
┌─────────────────────────────────────┐
│  Context State (useContext)          │
│  - ThemeContext: theme, toggleTheme  │
│  - SocketContext: socket, isConnected│
└─────────────────────────────────────┘
              │
              │ For persistent data
              ▼
┌─────────────────────────────────────┐
│  Backend State (Database)            │
│  - Customers, orders, vehicles      │
│  - Fetched via API calls            │
└─────────────────────────────────────┘
```

---

## 📚 Learning Path for Your Classmate

### Week 1: Basics
1. ✅ Understand what React is (components, JSX)
2. ✅ Understand what Node.js/Express is (server, API)
3. ✅ Understand what MySQL is (database, tables)

### Week 2: Frontend
1. ✅ Study `HomePage.jsx` - Simple component
2. ✅ Study `LoginForm.jsx` - Form handling
3. ✅ Study `App.jsx` - Routing

### Week 3: Backend
1. ✅ Study `server.js` - How server starts
2. ✅ Study `routes/customers.js` - API endpoints
3. ✅ Study `controllers/customersController.js` - Business logic

### Week 4: Integration
1. ✅ Study `api/customers.js` - How frontend calls backend
2. ✅ Trace a complete flow (create customer)
3. ✅ Understand authentication flow

### Week 5: Advanced
1. ✅ Socket.io real-time updates
2. ✅ Context API (Theme, Socket)
3. ✅ AI chatbot integration

---

## 🎯 Key Questions to Test Understanding

1. **What happens when you click "Login"?**
   - Answer: Form → API call → Backend route → Controller → Database check → Token returned → Stored → Redirect

2. **How does dark mode work?**
   - Answer: ThemeToggle → ThemeContext → Updates state → Sets data-theme → CSS variables change → Components re-render

3. **How does real-time order tracking work?**
   - Answer: Order updated → Controller emits Socket.io event → All connected clients receive → Toast shows → UI updates

4. **What's the difference between a route and a controller?**
   - Answer: Route = URL definition, Controller = Business logic

5. **Why do we use controllers instead of putting logic in routes?**
   - Answer: Separation of concerns, reusability, easier testing

---

Use these diagrams and explanations to teach your classmate! 🎓

